import type React from "react";

// ponytail: blank placeholder, populate with course list when backend query endpoints exist
export const Courses: React.FC = () => (
  <div className="p-8">
    <h1>My Courses</h1>
    <p>Your courses will appear here.</p>
  </div>
);
